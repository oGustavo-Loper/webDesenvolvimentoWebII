import { Pipe, PipeTransform } from '@angular/core'

@Pipe({
    name: 'nomecracha'
})

export class NomeCrachaPipe implements PipeTransform{
    transform(value: any, ...args: any[]) {
        
    }
}